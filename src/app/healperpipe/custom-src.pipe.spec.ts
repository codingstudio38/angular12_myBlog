import { CustomSrcPipe } from './custom-src.pipe';

describe('CustomSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
